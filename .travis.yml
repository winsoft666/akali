language: cpp
sudo: required
branches:
  only:
    - master

matrix:
  include:
    - os: linux
      compiler: gcc
      install: 
        - cmake --version
      script: 
        - cmake . -DBUILD_SHARED_LIBS=ON
        - make
        - make install
        
    - os: linux
      compiler: gcc
      install: 
        - cmake --version
      script: 
        - cmake . -DBUILD_SHARED_LIBS=OFF
        - make
        - make install

    - os: osx
      osx_image: xcode8
      env:
        - MATRIX_EVAL="CC=gcc-6 && CXX=g++-6"
      install: 
        - cmake --version
      script: 
        - cmake . -DBUILD_SHARED_LIBS=ON
        - make
        - make install
        
    - os: osx
      osx_image: xcode8
      env:
        - MATRIX_EVAL="CC=gcc-6 && CXX=g++-6"
      install: 
        - cmake --version
      script: 
        - cmake . -DBUILD_SHARED_LIBS=OFF
        - make
        - make install

